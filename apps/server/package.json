{
  "name": "@gadget-wallet/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --env-file=../../.env --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target=bun",
    "build:vercel": "bun build src/app.ts --outfile dist/app.cjs --target=node --format=cjs --minify --define:process.env.NODE_ENV='\"production\"'",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@gadget-wallet/db": "workspace:*",
    "@gadget-wallet/types": "workspace:*",
    "@hono/zod-validator": "^0.2.0",
    "@supabase/supabase-js": "^2.111.0",
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "^0.33.0",
    "hono": "^4.5.0",
    "jsonwebtoken": "^9.0.0",
    "postgres": "^3.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.5.0"
  }
}
